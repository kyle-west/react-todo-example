import React from 'react'
import './styles.css'

export default function TodoItem({ label, checked, onClick }) {
  return (
    <li className="TodoItem">
      {/* TODO: Render a single todo item with a checkbox */}
    </li>
  )
}